# ArtBench Data

Expected files:

- `latents/artbench256/train_latents.npz`
- `latents/artbench256/test_latents.npz`
- `hf_artbench/train/` if using the diffusers training wrapper
- `indices/` for any ArtBench subset index files

