# ArtBench refine runs

Default setup uses the ArtBench latent pipeline and `baroque` prompt.

The current legacy counterfactual and LDS engines are CIFAR-specific, so this scaffold includes ArtBench folders and placeholder metric entrypoints until an ArtBench metric engine is added.

